package com.example.aplicatieandroidip;

public class LoginResponse {
    private String token; // This should match the JSON key for the JWT token

    public String getToken() {
        return token;
    }
}